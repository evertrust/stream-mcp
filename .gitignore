# Secrets / local env — NEVER commit
.env
.env.*
!.env.example
# Local MCP client config (may carry credentials in its env block)
.mcp.json

# Dependencies
node_modules/
package-lock.json
.npmrc

# Local working notes under docs/ subfolders (never committed)
docs/*/

# Build output
dist/
*.tsbuildinfo
# Transient bun --compile scratch files (build:binary / build:binaries)
*.bun-build

# Test / coverage
coverage/
.vitest/

# Logs
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Tooling caches
.ruff_cache/
.pytest_cache/
.eslintcache
